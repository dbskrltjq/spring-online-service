<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd" >
<configuration>
	<settings>
		<setting name="logImpl" value="LOG4J2" />
		<setting name="jdbcTypeForNull" value="NULL" />
	</settings>
	
	<typeAliases>
		<!-- 
			지정된 패키지에서 클래스를 검색해서 클래스의 이름을 소문자로 변환한 형태의 별칭으로 등록한다.
			vo클래스에 @Aliaus 어노테이션이 있으면 그 어노테이션으로 지정한 이름이 별칭으로 등록된다.
			
			mapper 파일 작성할 때 사용할 vo객체의 패키지명 포함없이 User로 적을 수 있다. 
			ex) <select parameterType="User">
		 -->
		<package name="kr.co.hta.vo" />	
	
	</typeAliases>
	
	

</configuration>